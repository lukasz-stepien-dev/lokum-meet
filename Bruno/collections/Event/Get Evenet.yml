info:
  name: Get Evenet
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/public/events/5
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
